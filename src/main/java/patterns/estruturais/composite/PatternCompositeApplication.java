package patterns.estruturais.composite;

import patterns.estruturais.composite.service.CompositeService;

/**
 * Classe principal para executar os exemplos do padrão Composite.
 */
public class PatternCompositeApplication {

    public static void main(String[] args) {
        // Executar os exemplos do padrão Composite
        CompositeService service = new CompositeService();
        service.executarExemplos();
    }
}
