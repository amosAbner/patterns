Bridge
======

Exemplo didático do padrão Bridge (Separação entre Abstração e Implementação).

Estrutura:
- abstraction: Contém as abstrações (RemoteControl e implementações concretas)
- implementation: Contém a interface Implementor (`Device`) e implementações concretas (TV, Radio)
- service: Demonstração de uso

