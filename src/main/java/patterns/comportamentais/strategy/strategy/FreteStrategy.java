package patterns.comportamentais.strategy.strategy;

/**
 * Interface Strategy. Declara o método comum para os algoritmos suportados.
 */
public interface FreteStrategy {
    double calcular(double peso);
}
